# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB_NAME=ergoflow_db

# Google Calendar OAuth (not yet implemented — mock service used)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:8000/auth/callback

# Agent Builder (future integration)
AGENT_BUILDER_ENDPOINT=
AGENT_BUILDER_API_KEY=
GOOGLE_CLOUD_PROJECT=ergoflow-ai-hackathon

# MongoDB MCP Server (Cloud Run URL — populated after mcp-server/ deployment)
MCP_SERVER_URL=
